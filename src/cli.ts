import chalk from 'chalk';
import ora from 'ora';
import { Command } from 'commander';
import { fetchTrending, fetchGainersNLosers } from './fetcher';

const log = console.log;
const cyan = chalk.cyan;
const pos = chalk.green;
const neg = chalk.red;

async function main() {}
